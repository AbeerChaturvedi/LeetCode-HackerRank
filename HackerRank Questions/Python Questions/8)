if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    
    max_Score = max(arr)
    while max_Score in arr:
        arr.remove(max_Score)
        
    print(max(arr))